server :
  port : 1000

spring :
  application :
    name : eureka-server

eureka :
  instance :
    hostname : localhost
  server:
    eviction-interval-timer-in-ms: 10000
  client :
    register-with-eureka: false
    fetch-registry: false
